{
  "name": "hello-alan",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "wrangler deploy --dry-run",
    "test": "node --test",
    "register:discord": "node --env-file=.env scripts/register-command.js"
  },
  "engines": { "node": ">=22" },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
